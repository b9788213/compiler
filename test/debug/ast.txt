Program
├── Func(name='main', args=[])
│   └── Call(name='print')
│       └── String(bir şey girin :)
├── Func(name='print', args=['s'])
│   ├── Asm(call len)
│   ├── Asm(mov rdx, rax)
│   ├── Asm(mov rax, 1)
│   ├── Asm(mov rsi, rdi)
│   ├── Asm(mov rdi, 1)
│   ├── Asm(syscall)
│   └── Asm(jmp .exit)
├── Func(name='input', args=[])
│   ├── Asm(mov rdi, 4096 ;bufsize)
│   ├── Asm(call alloc ;ptr rax'ta)
│   ├── Asm(mov rsi, rax; buffer)
│   ├── Asm(xor rax, rax ;write)
│   ├── Asm(xor rdi, rdi ;stdin)
│   ├── Asm(mov rdx, 4096 ;bufsize)
│   └── Asm(syscall)
├── Func(name='len', args=['s'])
│   ├── Asm(xor rax, rax)
│   ├── Asm(.loop:)
│   ├── Asm(cmp byte [rdi + rax], 0)
│   ├── Asm(je .exit)
│   ├── Asm(inc rax)
│   └── Asm(jmp .loop)
└── Func(name='alloc', args=['i'])
    ├── Asm(mov rsi, rdi)
    ├── Asm(mov rax, 9)
    ├── Asm(xor rdi, rdi)
    ├── Asm(mov rdx, 0x1 | 0x2)
    ├── Asm(mov r10, 0x02 | 0x20)
    ├── Asm(mov r8, -1)
    ├── Asm(mov r9, 0)
    └── Asm(syscall)