Program
├── Func(name='main', args=[])
│   ├── Assign(name='x')
│   │   └── String(merhaba 
)
│   ├── Call(name='print')
│   │   └── Id(x)
│   └── Call(name='print')
│       └── Id(x)
├── Func(name='print', args=['s'])
│   ├── Asm(call len)
│   ├── Asm(mov rdx, rax)
│   ├── Asm(mov rax, 1)
│   ├── Asm(mov rsi, rdi)
│   ├── Asm(mov rdi, 1)
│   ├── Asm(syscall)
│   └── Asm(jmp .exit)
└── Func(name='len', args=['s'])
    ├── Asm(xor rax, rax)
    ├── Asm(.loop:)
    ├── Asm(cmp byte [rdi + rax], 0)
    ├── Asm(je .exit)
    ├── Asm(inc rax)
    └── Asm(jmp .loop)