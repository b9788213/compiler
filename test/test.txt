import test

fn hesapla(r, carpant):
    sonuc = PI * r * r * -5
    return sonuc * carpant + "merhaba"

fn main():
    yari_cap = 10
    merhaba(1+2, 3-4)
    final = hesapla(yari_cap, 2.7 + 3)
    return final